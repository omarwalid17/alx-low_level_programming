#include "main.h"
/**
*add - a function to check if c is lowercase
*
* @a : First Input
* @b : Second Input
*Return: Always 0 (Success)
*/

int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
